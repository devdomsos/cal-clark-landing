# Claude Code

This file exists so Claude Code loads the same instructions as Cursor.
The source of truth is [`AGENTS.md`](AGENTS.md).
Do not add project rules here that are missing from AGENTS.md.

Claude skills are under `.claude/skills/` (already filled when you copy this kit).
If you later edit a skill under `.cursor/skills/`, copy that folder over `.claude/skills/` (or run `scripts/sync-skills-to-claude.sh`).

@AGENTS.md
